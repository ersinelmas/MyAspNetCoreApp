@using MyAspNetCoreApp.Web.Controllers
@{
    ViewData["Title"] = "Index";
}

@model List<Product2>

<h1>Example Controller Index Page</h1>

@foreach (var item in Model)
{
    <p>@item.Id @item.Name</p>
}